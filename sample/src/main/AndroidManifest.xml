<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.fengjr.sample"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name=".App"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activities.BrowserActivity"/>
        <activity android:name=".activities.user.LoginActivity"/>
        <activity android:name=".activities.user.RegisterActivity"/>
        <activity android:name=".activities.user.UserInfoActivity"/>
        <activity android:name=".activities.production.ProductionActivity"/>
        <activity android:name=".activities.production.TradeActivity"/>

    </application>

</manifest>